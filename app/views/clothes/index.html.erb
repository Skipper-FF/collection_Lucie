<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h1>All Clothes</h1>
      <ul class="list-group">
        <% @clothes.each do |clothe| %>
          <li class="list-group-item">
     <!-->       <%= link_to clothe.name, new_season_clothe_path(clothe) %>   -->
          </li>
        <% end %>
      </ul>
      <%= link_to "Add a new Cloth", season_add_path(@season), class: 'btn btn-success' %>
    </div>
  </div>
</div>
