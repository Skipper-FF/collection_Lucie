<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h1>Sélectionnez ou ajoutez une saison</h1>
      <div class="cards">
        <% @seasons.each do |season| %>
          <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path season.photo.key, height: 300, width: 400, crop: :fill %>')">
            <%= link_to season.name, season_path(season) %>
          </div>
        <% end %>
      </div>
      <%= link_to "Ajouter une saison", new_season_path, class: 'btn btn-primary' %>
    </div>
  </div>
</div>


